import { Outlet } from "react-router-dom";
import Notification from './Noti.js';

const Layout = () => {
    return (
        
            
            <Outlet />
      
    );
};

export default Layout;
